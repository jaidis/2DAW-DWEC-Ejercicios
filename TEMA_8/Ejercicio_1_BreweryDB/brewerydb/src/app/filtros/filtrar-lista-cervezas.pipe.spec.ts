import { FiltrarListaCervezasPipe } from './filtrar-lista-cervezas.pipe';

describe('FiltrarListaCervezasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarListaCervezasPipe();
    expect(pipe).toBeTruthy();
  });
});
